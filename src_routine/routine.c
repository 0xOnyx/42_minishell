#include "minishell.h"
#include <stdio.h>


grep -a " " | cat < ;


int	main(int argc, char **argv, char **env)
{
	int i;
	int	fd[2];
	int tmp_fd;

	i = 0;
	tmp_fd = dup(1);
	while (argv[i])
	{
		

	}

	return (0);
}